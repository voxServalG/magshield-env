# Input Examples

`minimal-finite-element` is a complete strict build definition with a two-point,
two-channel CSV field source. `geometry` shows equivalent CSV and inline-ASCII
VTP path contracts plus ordered channel metadata. They are deliberately small
contract fixtures, not physically optimized coil designs.

Use the live `magshield-env --help` output for current commands and arguments.
