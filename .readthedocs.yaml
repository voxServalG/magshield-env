version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    post_install:
      - python -m sphinx_intl build --locale-dir site/locales

sphinx:
  configuration: site/conf.py
  fail_on_warning: true

python:
  install:
    - requirements: site/requirements.txt
